interface:
  display_name: "Visor Mobile App Agent"
  short_description: "Discover, navigate, and repair mobile app maps"
  default_prompt: "Use $visor-discovery through the installed Navigator agent to operate the mobile device, recover deliberately, and keep semantic map routes current."
