interface:
  display_name: "Visor Mobile App Agent"
  short_description: "Discover, navigate, and repair mobile app maps"
  default_prompt: "Use $visor-discovery to operate this running mobile app from compact map memory, discover unknown states safely, and execute known routes efficiently."
