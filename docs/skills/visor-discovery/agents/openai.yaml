interface:
  display_name: "Visor Discovery"
  short_description: "Build compact agent navigation memory"
  default_prompt: "Use $visor-discovery to learn this app agentically, persist compact semantic memory, verify safe deterministic routes, and report gaps."
